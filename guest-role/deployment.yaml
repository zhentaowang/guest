apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: guest-role
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: guest-role
    spec:
      containers:
        - name: guest-role
          image: java:1.8
          command: ["java -jar guest-role.jar"]
          volumeMounts:
          - mountPath: "/usr/local/airportjar"
            name: code
            subPath: guest/guest-role
          - mountPath: "/etc/localtime"
            name: tz-config
          ports:
            - containerPort: 8080
      volumes:
        - name: code
          persistentVolumeClaim:
            claimName: code-claims
        - name: tz-config
          hostPath:
            path: /etc/localtime
---
apiVersion: v1
kind: Service
metadata:
  name: guest-role
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: guest-role
