apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: guest-order
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: guest-order
    spec:
      containers:
        - name: guest-order
          image: java:8-jre-alpine
          command: ["java","-jar","/code/guest-order-1.0-SNAPSHOT.jar"]
          volumeMounts:
          - mountPath: "/code"
            name: code
            subPath: guest/guest-order
          ports:
            - containerPort: 8080
      volumes:
        - name: code
          persistentVolumeClaim:
            claimName: code-claims
---
apiVersion: v1
kind: Service
metadata:
  name: guest-order
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: guest-order