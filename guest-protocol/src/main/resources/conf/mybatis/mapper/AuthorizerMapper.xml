<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhiweicloud.guest.mapper.AuthorizerMapper">
  <resultMap id="BaseResultMap" type="com.zhiweicloud.guest.model.Authorizer">
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="airport_code" jdbcType="VARCHAR" property="airportCode" />
    <result column="protocol_id" jdbcType="BIGINT" property="protocolId" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="cellphone" jdbcType="VARCHAR" property="cellphone" />
    <result column="telephone" jdbcType="VARCHAR" property="telephone" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
    <result column="is_deleted" jdbcType="SMALLINT" property="isDeleted" />
  </resultMap>

  <sql id="Base_Column_List">
    id, airport_code, protocol_id, name, cellphone, telephone, create_time, update_time, 
    is_deleted
  </sql>

  <update id="updateByIdAndAirportCode" parameterType="com.zhiweicloud.guest.model.Authorizer">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    update authorizer
    <set>
      <if test="name != null">
        name = #{name,jdbcType=VARCHAR},
      </if>
      <if test="cellphone != null">
        cellphone = #{cellphone,jdbcType=VARCHAR},
      </if>
      <if test="telephone != null">
        telephone = #{telephone,jdbcType=VARCHAR},
      </if>
      <if test="isDeleted != null">
        is_deleted = #{isDeleted,jdbcType=SMALLINT},
      </if>
      update_time = CURRENT_TIMESTAMP
    </set>
    where airport_code = #{airportCode,jdbcType=VARCHAR}
    <if test="protocolId != null">
      and protocol_id = #{protocolId,jdbcType=BIGINT}
    </if>
    <if test="id != null">
      and id = #{id,jdbcType=BIGINT}
    </if>
  </update>

  <select id="getListByConidition" parameterType="map" resultMap="BaseResultMap">
    SELECT
    id,name,cellphone,telephone
    FROM authorizer
    where airport_code = #{airportCode,jdbcType=VARCHAR} and is_deleted = 0
    <if test="protocolId != null" >
      and protocol_id = #{protocolId,jdbcType=BIGINT}
    </if>
    order by id desc
  </select>

  <select id="getListCount" parameterType="map" resultType="int">
    SELECT
    count(id)
    FROM authorizer
    where airport_code = #{airportCode,jdbcType=VARCHAR} and is_deleted = 0
    <if test="protocolId != null" >
      and protocol_id = #{protocolId,jdbcType=BIGINT}
    </if>
  </select>

  <select id="selectById" parameterType="map" resultMap="BaseResultMap">
    SELECT
    name,cellphone,telephone
    FROM authorizer
    where airport_code = #{airportCode,jdbcType=VARCHAR} and id = #{id,jdbcType=BIGINT} and is_deleted = 0
  </select>

  <select id="deleteByIdAndAirportCode" parameterType="map">
    update authorizer set is_deleted = 1 where id not in (${ids}) and airport_code = #{airportCode,jdbcType=VARCHAR} and protocol_id = #{protocolId,jdbcType=BIGINT}
  </select>
</mapper>