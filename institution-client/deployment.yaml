apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: institution-client
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: institution-client
    spec:
      containers:
        - name: institution-client
          image: java:8
          command: ["java", "-jar", "java/institution-client.jar"]
          volumeMounts:
          - mountPath: "/java"
            name: code
            subPath: guest/institution-client
          - mountPath: "/etc/localtime"
            name: tz-config
          ports:
            - containerPort: 8080
      volumes:
        - name: code
          persistentVolumeClaim:
            claimName: code-claims
        - name: tz-config
          hostPath:
            path: /etc/localtime
---
apiVersion: v1
kind: Service
metadata:
  name: institution-client
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: institution-client
